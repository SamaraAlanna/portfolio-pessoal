---
slug: voge-brasil
titulo: VOGE Brasil
tipo: engenharia
tags: [Front-End, Back-End]
estado: no-ar
destaque: true
ordem: 3
ordemHome: 0
publicado: true
imagem: /imagens/projetos/voge-brasil.webp
heroCase: /imagens/cases/voge-telas.webp
descricao: Site institucional de motos premium em WordPress. Validação de CPF, telefone e e-mail escrita do zero, e integração de leads com o CRM que cai para e-mail quando a API não confirma.
resumo: Marca premium de motos entrando no Brasil. Fiz o sistema de formulários com validação sem biblioteca, a integração de leads com o CRM e a rede de concessionárias, em dupla com outro desenvolvedor.
links:
  - Ver o site no ar | https://vogebrasil.com.br/
ficha:
  - PAPEL | Front-end, validações e integrações
  - CLIENTE | VOGE Brasil, via TecSinapse
  - STACK | WordPress, PHP, JS vanilla, API REST, Git
  - TIME | Em dupla com Paulo Azevedo
  - PERÍODO | 2026
---

{/* REGRA DE ANINHAMENTO: o bloco de fora precisa de MAIS dois-pontos que o de dentro.
    Uma seção sem nada aninhado usa três. Com um bloco dentro, quatro. */}

{/* RÓTULOS EM CAIXA NORMAL: a caixa alta vem do `text-transform` do `bloco-secao`. */}

::::secao{rotulo="Formulários" titulo="Três validações escritas do zero, sem biblioteca"}
Escrevi a validação em JavaScript puro: CPF com dígitos verificadores e máscara própria, telefone com regra de DDD e e-mail com heurística contra endereço falso. Somei honeypot, proteção contra duplo envio e uma segunda camada em PHP no servidor.

:::codigo{arquivo="validacao.js" abas="CPF, Máscara" prova="true"}
```js
function validarCPF(cpf) {
  cpf = cpf.replace(/\D/g, '');
  if (cpf.length !== 11) return false;
  if (/^(\d)\1{10}$/.test(cpf)) return false;

  let soma = 0, resto;
  for (let i = 0; i < 9; i++) soma += parseInt(cpf[i]) * (10 - i);
  resto = soma % 11;
  if ((resto < 2 ? 0 : 11 - resto) !== parseInt(cpf[9])) return false;

  soma = 0;
  for (let i = 0; i < 10; i++) soma += parseInt(cpf[i]) * (11 - i);
  resto = soma % 11;
  return (resto < 2 ? 0 : 11 - resto) === parseInt(cpf[10]);
}
```

```js
function maskCPF(v) {
  v = v.replace(/\D/g, '').slice(0, 11);
  return v
    .replace(/(\d{3})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d)/, '$1.$2')
    .replace(/(\d{3})(\d{1,2})$/, '$1-$2');
}
```
:::
::::

::::secao{rotulo="Integração" titulo="O lead tem dois caminhos até o CRM"}
Os formulários mandam o lead ao CRM da marca por API REST, roteado para a concessionária escolhida. O sistema lê a confirmação da API e, quando ela não vem, envia o lead por e-mail.

:::diagrama{desvio="se falhar | envio por e-mail"}
formulário
validação
roteamento por unidade
CRM | ciano
confirmação 2xx
:::
::::

:::secao{rotulo="Concessionárias" titulo="Um arquivo de dados controla a rede inteira"}
A página de concessionárias é um sistema de busca completo: filtro em cascata por estado e cidade, mapa, contador de resultado e estado de nenhum resultado. As lojas ficam num arquivo de dados e os cards são montados sozinhos. Ativar uma unidade ou mudar o status dela é editar um campo, e isso reflete na página e nos formulários. O filtro por estado e cidade está pronto e desligado: enquanto a marca inaugura as primeiras unidades, a versão pública mostra só os cards, e ele entra em operação quando a rede crescer, sem novo desenvolvimento.
:::

::::secao{rotulo="Seletor de manual" titulo="Um seletor em dois níveis que nunca fica vazio"}
Na página de Serviços, a pessoa escolhe a categoria, Adventure ou Scooter, e a lista de modelos se atualiza já com um padrão marcado. A imagem e o download acompanham a escolha.

:::imagens{colunas="1"}
![Seletor de manual na página de Serviços](/imagens/cases/voge-seletor-manual.webp)
:::
::::
